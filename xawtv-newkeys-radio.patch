diff -urN xawtv-3.74/console/radio.c xawtv-3.74.new/console/radio.c
--- xawtv-3.74/console/radio.c	Tue Jan 22 15:10:44 2002
+++ xawtv-3.74.new/console/radio.c	Sat May  4 21:19:11 2002
@@ -628,6 +628,18 @@
 		mvwprintw(wcommand, 1, 2, "Go to preset station %d", i+1);
 	    }
 	    break;
+	case 'm':
+	case 'M':
+	    if (mute == 0) {
+		mute = 1;
+	        mvwprintw(wcommand, 1, 2, "Mute");
+		radio_mute(fd);
+	    } else {
+		mute = 0;
+		radio_unmute(fd);
+	        mvwprintw(wcommand, 1, 2, "Unmute");
+	    }
+	    break;
 	case 'L' & 0x1f:  /* Ctrl-L */
 	    redrawwin(stdscr);
 	    redrawwin(wfreq);
